---
interface Props {
  text: string;
  className?: string;
}

const { text, className } = Astro.props;
---

<button class={`btn btn-outline btn-primary normal-case ${className}`}
  >{text}</button
>
